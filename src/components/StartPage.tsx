import { useNavigate } from "react-router";

function StartPage() {
    let navigate = useNavigate();
    
    return (
        <div>
           WORK IN PROGRESS
        </div>
    )
}

export default StartPage;